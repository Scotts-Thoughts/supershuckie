diff --git a/src/video_core/rasterizer_interface.h b/src/video_core/rasterizer_interface.h
index 9306cb3..00f2179 100644
--- a/src/video_core/rasterizer_interface.h
+++ b/src/video_core/rasterizer_interface.h
@@ -19,6 +19,12 @@ struct MemoryFillConfig;
 
 namespace VideoCore {
 
+/// Super Shuckie: when set, the hardware rasterizer accepts every draw batch and draws nothing
+/// (the frontend uses it while seeking through a replay, where only the CPU-side state
+/// matters). Display transfers, fills and the texture cache keep working, so the emulated
+/// program's memory is unaffected; only the picture is stale until it is cleared.
+inline std::atomic<bool> g_skip_drawing{false};
+
 enum class LoadCallbackStage {
     Prepare,
     Preload,
diff --git a/src/video_core/renderer_opengl/gl_rasterizer.cpp b/src/video_core/renderer_opengl/gl_rasterizer.cpp
index 1791506..1e49735 100644
--- a/src/video_core/renderer_opengl/gl_rasterizer.cpp
+++ b/src/video_core/renderer_opengl/gl_rasterizer.cpp
@@ -467,6 +467,9 @@ bool RasterizerOpenGL::SetupGeometryShader() {
 }
 
 bool RasterizerOpenGL::AccelerateDrawBatch(bool is_indexed) {
+    if (VideoCore::g_skip_drawing.load(std::memory_order_relaxed)) {
+        return true; // Super Shuckie: consumed, nothing drawn, no CPU vertex processing either
+    }
     if (regs.pipeline.use_gs != Pica::PipelineRegs::UseGS::No) {
         if (regs.pipeline.gs_config.mode != Pica::PipelineRegs::GSMode::Point) {
             return false;
@@ -545,6 +548,10 @@ bool RasterizerOpenGL::AccelerateDrawBatchInternal(bool is_indexed) {
 }
 
 void RasterizerOpenGL::DrawTriangles() {
+    if (VideoCore::g_skip_drawing.load(std::memory_order_relaxed)) {
+        vertex_batch.clear(); // Super Shuckie
+        return;
+    }
     if (vertex_batch.empty())
         return;
     Draw(false, false);
