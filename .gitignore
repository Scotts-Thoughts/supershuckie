# This is the build folder
/target
build
build-static

# Don't want to accidentally commit someone's personal config
.idea
.vscode

# These things...
.DS_Store

# macOS might need libMoltenVK.dylib; avoid checking it in by mistake
*.dylib

# Build logs from manual Windows builds
build_*.log

# Runtime user data
UserData/

# Local packaging output (built exe / zips) - never commit binaries
/dist
/dist-static-test
