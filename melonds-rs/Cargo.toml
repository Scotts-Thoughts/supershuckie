[package]
name = "melonds-rs"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[dependencies]

[build-dependencies]
cc = { workspace = true }

[features]
# Links melonDS's static libraries (libcore.a, libteakra.a) and the C++ runtime, so a plain
# `cargo test`/`cargo build` of this crate's dependents can link without CMake/corrosion or
# the frame server's build.rs around to supply them. See build.rs.
link-cores = []
